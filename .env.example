# Eduvia Server Environment Configuration
PORT=3001
NODE_ENV=development

# Allowed CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Optional custom SQLite database path (defaults to server/data/eduvia.db)
# DATABASE_PATH=server/data/eduvia.db
